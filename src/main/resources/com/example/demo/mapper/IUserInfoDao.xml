<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.login.dao.IUserInfoDao">

	<select id="getUser" resultType="com.example.demo.login.vo.UserInfoVO">
		SELECT 
		  * 
		FROM
		  user_t t 
		WHERE t.name =#{user.name}
	</select>
	<select id="userNameIsExist" resultType="int">
		SELECT 
		  COUNT(*) 
		FROM
		  user_t t 
		WHERE t.name = #{userName}
	</select>

	<insert id="insertUser" useGeneratedKeys="true" keyProperty="id" keyColumn="id" parameterType="com.example.demo.login.vo.UserInfoVO">
		INSERT INTO user_t (
		  id,
		  PASSWORD,
		  NAME,
		  creater_by,
		  create_date
		) 
		VALUES
		  (
		    #{id},
		    #{passWord},
		    #{name},
		    #{id},
		    now()
		  )
	</insert>
	<select id="getDepartments" resultType="com.example.demo.login.vo.DepartmentVo">
		SELECT 
		  d.`id` AS id,
		  d.`department_code` AS departmentCode,
		  d.`is_exist_childen` AS isexistChilden,
		  d.`parent_id` AS parentId,
		  d.`department_level` AS departmentLevel,
		  d.`creater_by` AS createrBy,
		  d.`create_date` AS createDate,
		  d.`last_update_by` AS lastupDateBy,
		  d.`last_update_date` AS lastupDateDate 
		FROM
		  department_t d 
		ORDER BY d.`department_level` DESC,
		  d.`parent_id`  
	</select>
	
	
</mapper>